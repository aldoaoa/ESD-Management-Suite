# secrets.example.toml — DO NOT COMMIT real secrets to git.
# Copy this file to .streamlit\secrets.toml and fill values before running Streamlit locally.

# Supabase (replace with your project values)
SUPABASE_URL = "https://your-project-ref.supabase.co"
SUPABASE_KEY = "your-supabase-anon-or-service-key"

# Optional: other secrets used by the app
#ANOTHER_SECRET = "value"
